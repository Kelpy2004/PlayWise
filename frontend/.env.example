# Use /api when frontend and API share same domain (Vercel rewrite / Render fullstack).
# For separate frontend hosting, set full live API URL, e.g. https://playwise-cda1.onrender.com/api
VITE_API_BASE=/api
VITE_SENTRY_DSN=
VITE_SENTRY_TRACES_SAMPLE_RATE=0.15
